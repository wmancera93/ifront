import { DataTablePipe } from './data-table.pipe';

describe('DataTablePipe', () => {
  it('create an instance', () => {
    const pipe = new DataTablePipe();
    expect(pipe).toBeTruthy();
  });
});
